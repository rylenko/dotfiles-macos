#!/bin/sh

[ -z "$1" ] && echo "Set files extension to replace" && exit 1
[ -z "$2" ] && echo "Set what to replace" && exit 2
[ -z "$3" ] && echo "Set replacement" && exit 3

find . -type f -name "*.$1" -print0 | xargs -0 sed -i '' -e 's/$2/$3/g'
